name: innoliber
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
 
  - python=3.11

  # PyTorch CUDA 12.6
  - pytorch=2.5.1
  - torchvision=0.20.1
  - torchaudio=2.5.1
  - pytorch-cuda=12.6

  - numpy=1.24.3
  - scikit-learn=1.3.2
  - pandas=2.1.3

  # Poetry
  - pip

  - pip:

    - poetry==1.7.1

    # Web Framework (updated to match pyproject.toml)
    - fastapi==0.118.2
    - uvicorn[standard]==0.31.0
    - python-multipart==0.0.12

    # Database (updated to match pyproject.toml)
    - sqlalchemy[asyncio]==2.0.35
    - asyncpg==0.29.0
    - alembic==1.13.3
    - pgvector==0.2.4

    # Cache & Queue (updated to match pyproject.toml)
    - redis==5.2.0
    - hiredis==2.2.3
    - celery==5.4.0

    # Validation (updated to match pyproject.toml)
    - pydantic[email]==2.9.2
    - pydantic-settings==2.6.0

    # Auth
    - python-jose[cryptography]==3.3.0
    - passlib[bcrypt]==1.7.4

    # AI/LLM (updated to match pyproject.toml)
    - openai==1.55.0
    - httpx==0.27.2

    # Development Tools (updated to match pyproject.toml)
    - black==24.10.0
    - isort==5.13.2
    - flake8==7.1.1
    - mypy==1.13.0
    - pytest==8.3.3
    - pytest-asyncio==0.24.0
    - pytest-cov==6.0.0
